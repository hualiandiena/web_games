import widget from "../Widget.js";

function Carouse(props) {

    // 暂时使用
    var template = '<div>dsfs</div>';

    let carouse = Object.create(widget);

    carouse.render = function() {
        
    }

    return carouse;
}

export default Carouse;