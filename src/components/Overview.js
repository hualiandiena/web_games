import { createElement } from "../widgets.js";


export default function Overview() {
    var template = '<div>' + 
            '<ul>' + 
            '</ul>' +
        '</div>';
    return createElement(template, {});
}